.container
  %h1 Feedbacks
  
  %table.table
    %tr
      %th 
        Feedback
      %th       
        Control

  
    - @feedbacks.each do |feedback|
      %tr
        %td= link_to simple_format(feedback.content), edit_feedback_path(feedback)
        %td= link_to 'Destroy', feedback, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
  
  %br
  
  = link_to 'New Feedback', new_feedback_path, class: 'btn'
