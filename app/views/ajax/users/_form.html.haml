= form_for @ajax_user do |f|
  - if @ajax_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ajax_user.errors.count, "error")} prohibited this ajax_user from being saved:"
      %ul
        - @ajax_user.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
