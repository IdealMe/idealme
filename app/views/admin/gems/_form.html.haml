= form_for @admin_gem do |f|
  - if @admin_gem.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_gem.errors.count, "error")} prohibited this admin_gem from being saved:"
      %ul
        - @admin_gem.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
