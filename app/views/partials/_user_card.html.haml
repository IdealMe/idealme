.user-card                                                    
  .avatar
    = link_to image_tag(user.avatar.url(:square)), user_path(user)
  = link_to user.fullname, user_path(user), :class => 'user-name'